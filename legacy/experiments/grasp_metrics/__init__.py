"""Entry points. The package under src/ is library code and holds no CLI state;
everything runnable is invoked from here, so `make <target>` and
`python -m experiments.<name>` are the only two ways to start anything.
"""
